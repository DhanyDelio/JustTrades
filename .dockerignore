# Git
.git
.gitignore
.github

# Python cache
__pycache__
*.pyc
*.pyo

# Environment (injected via docker-compose env_file)
.env
.env.example

# OS files
.DS_Store

# Data cache (downloaded at runtime if needed)
data_cache

# IDE / Editor
.vscode
.idea
.agents

# Tests (not needed in production image)
tests

# Analysis & charts (not needed in production)
analysis
charts

# ML models are copied in at build time, but training output
# should be mounted as a volume so it persists
